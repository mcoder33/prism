{
  "name": "@itfinance/prism",
  "version": "0.1.0",
  "description": "PRISM — recursive decomposition workflow for AI coding agents. Installs /prism slash commands into any project for Claude Code, Cursor, Codex, Gemini CLI and others.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "prism": "./bin/prism.js"
  },
  "files": [
    "bin",
    "dist",
    "templates",
    "README.md"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "vitest run",
    "prepublishOnly": "npm run build",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@inquirer/prompts": "^7.2.0",
    "commander": "^14.0.0",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
